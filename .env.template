# ============================================================
# TCG ENHANCED BACKEND v2.0 — ENVIRONMENT SETUP
# Complete Enterprise System Configuration
# ============================================================

# SERVER
PORT=3000

# ── FIREBASE REALTIME DATABASE ──
FIREBASE_DB_URL=https://babysitter-b322c-default-rtdb.firebaseio.com
FIREBASE_API_KEY=your_firebase_api_key
FIREBASE_AUTH_DOMAIN=your_firebase_auth_domain
FIREBASE_PROJECT_ID=babysitter-b322c
FIREBASE_STORAGE_BUCKET=your_firebase_storage_bucket
GOOGLE_APPLICATION_CREDENTIALS={"type":"service_account","project_id":"babysitter-b322c",...}

# ── EMAIL CONFIGURATION (Gmail/SMTP) ──
EMAIL_SERVICE=gmail
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-app-password
EMAIL_FROM=Gary Amick <gary.amick0614@gmail.com>

# Gmail Setup:
# 1. Enable 2-Factor Authentication
# 2. Create App Password: https://myaccount.google.com/apppasswords
# 3. Use 16-char password above (EMAIL_PASS)

# ── SMS CONFIGURATION (Twilio) ──
TWILIO_ACCOUNT_SID=your_twilio_account_sid
TWILIO_AUTH_TOKEN=your_twilio_auth_token
TWILIO_PHONE_NUMBER=+1234567890

# Twilio Setup:
# 1. Sign up: https://www.twilio.com/
# 2. Get free trial number
# 3. Get SID & Auth Token from console

# ── PAYMENT PROCESSING (Stripe) ──
STRIPE_SECRET_KEY=sk_live_your_stripe_key
STRIPE_PUBLISHABLE_KEY=pk_live_your_stripe_key
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret

# Stripe Setup:
# 1. Sign up: https://stripe.com/
# 2. Get API keys from https://dashboard.stripe.com/apikeys
# 3. Set up webhook for payment events

# ── AI PROVIDER CONFIGURATION ──
# System tries providers in order: Ollama → LM Studio → Groq → Together.ai

# 1. OLLAMA (Local, free, off-grid)
OLLAMA_URL=http://localhost:11434

# 2. LM STUDIO (Local, free, Windows/Mac/Linux)
LMSTUDIO_URL=http://localhost:1234
LMSTUDIO_MODEL=qwen2.5-7b-instruct

# 3. GROQ (Free tier, high quality)
GROQ_API_KEY=your_groq_api_key

# 4. TOGETHER.AI (Free tier)
TOGETHER_API_KEY=your_together_api_key

# ── OPTIONAL: TomTom Traffic API ──
TOMTOM_API_KEY=your_tomtom_api_key

# ============================================================
# SETUP INSTRUCTIONS
# ============================================================

# EMAIL SETUP (Required for notifications):
# 1. Use Gmail (free) or your email provider
# 2. If Gmail: https://myaccount.google.com/apppasswords
# 3. Generate 16-char app password
# 4. Use in EMAIL_PASS above
#
# SMS SETUP (Optional but recommended):
# 1. Sign up free at https://www.twilio.com/
# 2. Get account SID & auth token
# 3. Get trial phone number
# 4. Add to TWILIO_* above
#
# PAYMENTS (Optional for invoicing):
# 1. Sign up at https://stripe.com/
# 2. Get live API keys (or test for development)
# 3. Set webhook endpoint in Stripe dashboard
#
# AI MODELS (Required for chat):
# Option A: Local (recommended, offline, fastest)
#   - Install Ollama: https://ollama.ai/download
#   - Or LM Studio: https://lmstudio.ai
# 
# Option B: Free cloud APIs (fallback)
#   - Groq: https://console.groq.com/keys
#   - Together.ai: https://api.together.xyz
#
# ============================================================

